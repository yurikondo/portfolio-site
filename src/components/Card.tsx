import React from "react";
import VanillaTilt from "vanilla-tilt";

const Card = () => {
  return (
    <div
      className="border-solid border-2 border-sky-500 rounded-xl shadow-lg w-96 h-96 bg-black"
      data-tilt
      // data-tilt-glare="true"
      // data-tilt-max="1"
    >
      Card
    </div>
  );
};

export default Card;
